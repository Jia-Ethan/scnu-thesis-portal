# SC-TH 首页视觉收敛验收记录

本轮只处理首页视觉收敛与仓库主线整理，不改预检规则、后端解析和 Word 导出链路。

## 验收范围

- 删除首页可见大标题，仅保留 `SC-TH`
- 收窄主输入框高度、按钮尺寸、阴影和边框存在感
- 默认提示更新为 `按 Cmd/Ctrl + Enter 开始预检`
- 文本区改为受控扩展，超过阈值后内部滚动
- README / docs / 首页截图同步更新
- 根目录历史与本地产物目录继续降权

## 验收结果

- 桌面端首页主视觉已收敛为 `SC-TH + 单条输入框`
- 文件态与文本态均保持现有预检/导出链路不变
- 长文本不会再快速把首屏撑成大面板
- README 与 docs 已同步到本轮首页样式
- 根目录不再跟踪 `archive/`、`outputs/`、`tmp/` 的占位文件
